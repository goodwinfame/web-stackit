export enum viewState {
    none = "none", 
    list = "list", 
    detail = "detail"
};
export enum inputState {
    none = "none", 
    typing = "typing"
}
export enum listState {
    none = "none", 
    loading = "loading", 
    hasnext = "hasnext"
}
export enum detailState {
    none = "none", 
    loading = "loading"
}
