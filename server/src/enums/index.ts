

export enum sortType {
    activity = "activity",
    votes = "votes",
    creation = "creation",
    relevance = "relevance"
}

export enum orderType {
    desc = "desc",
    asc = "asc",
}
export enum acceptType {
    'true' = 'True',
    'false' = 'False'
}